// for the header button
var buttonHeader = document.getElementById("");

// Add a click event listener
buttonHeader.addEventListener("click", function () {
  // Change the button's color
  this.style.backgroundColor = "";
  this.style.color = "";
});

// for the nav text
var nav = document.getElementById("");

// Add a click event listener
nav.addEventListener("click", function () {
  // Change the nav color
  this.style.color = "";
});

// for buttons in the main
var button = document.getElementById("");

// Add a click event listener
button.addEventListener("click", function () {
  // Change the button's color
  this.style.backgroundColor = "";
  this.style.color = "";
});

// for the 3 logo in the footer
var logo = document.getElementById("");
// Add a click event listener
logo.addEventListener("click", function () {
  // Change the button's color
  this.style.backgroundColor = "";
});

// for the FAQ text bar
var bar = document.getElementById("");
// Add a click event listener
bar.addEventListener("click", function () {
  // Change the button's color
  this.style.backgroundColor = "";
});
